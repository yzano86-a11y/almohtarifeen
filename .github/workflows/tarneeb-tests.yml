name: Tarneeb Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tarneeb-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tarneeb-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Run Tarneeb engine acceptance suite
        run: node tests/tarneeb_engine.test.mjs
      - name: Run unified Tarneeb adapter acceptance suite
        run: node tests/tarneeb_adapter.test.mjs
