const SUITS=new Set(['♠','♥','♦','♣']);
const splitCard=(value)=>{const t=value.trim();const suit=[...t].findLast(ch=>SUITS.has(ch));if(!suit)return null;const pos=t.lastIndexOf(suit);return{rank:t.slice(0,pos),suit}};
function decorateHand(){document.querySelectorAll('#hand .cardplay').forEach(btn=>{if(btn.dataset.decorated)return;const c=splitCard(btn.textContent);if(!c)return;btn.textContent='';const r=document.createElement('span');r.className='card-rank';r.textContent=c.rank;const s=document.createElement('span');s.className='card-suit';s.textContent=c.suit;btn.append(r,s);btn.dataset.decorated='1';});}
function decorateTrick(){document.querySelectorAll('#trick .played').forEach(card=>{if(card.dataset.decorated)return;const raw=card.textContent.trim();const suit=[...raw].findLast(ch=>SUITS.has(ch));if(!suit)return;const pos=raw.lastIndexOf(suit);const rank=raw.slice(pos-1>=0?pos-1:0,pos);const label=raw.slice(0,pos-1).trim();card.textContent='';const l=document.createElement('small');l.textContent=label;const r=document.createElement('span');r.className='card-rank';r.textContent=rank;const s=document.createElement('span');s.className='card-suit';s.textContent=suit;card.append(l,r,s);card.dataset.decorated='1';});}
function enhance(){decorateHand();decorateTrick();}
const obs=new MutationObserver(enhance);obs.observe(document.body,{subtree:true,childList:true});enhance();
