{
  "name": "almohtarifeen-tarneeb",
  "version": "1.6.3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node tests/tarneeb_engine.test.mjs",
    "build": "rm -rf dist && mkdir -p dist && cp index.html app.js style.css ui-enhance.js ui-enhance.css pro-ui.css table-upgrade.css social-ui.css manifest.webmanifest sw.js dist/ && cp -R games dist/games",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "wrangler": "^4.0.0"
  }
}
