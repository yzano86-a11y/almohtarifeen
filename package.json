{
  "name": "almohtarifeen-tarneeb",
  "version": "1.6.4",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:engine": "node tests/tarneeb_engine.test.mjs",
    "test:adapter": "node tests/tarneeb_adapter.test.mjs",
    "test:ui": "node tests/ui_runtime_wiring.test.mjs",
    "build": "rm -rf dist && mkdir -p dist && cp index.html app.js style.css ui-enhance.js ui-enhance.css pro-ui.css social-ui.css reference-layout.css manifest.webmanifest sw.js dist/ && cp -R games dist/games && cp -R almolook dist/almolook",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "wrangler": "^4.0.0"
  }
}
